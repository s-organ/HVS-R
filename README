The HVS R file implements the method from scratch without external programming to see the operation of the method step by step, this is slow for large graphs. The SetSel_1.0.0.tar is an R package for this method that uses C++ versions of the code and is faster.
